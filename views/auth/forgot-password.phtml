<?php $this->useLayout($layout ?? 'dpress:layout') ?>
<?php $this->startBlock('content') ?>
    <h1><?= esc_html($title) ?></h1>
    <p>Enter the address you registered with and we will send a link to choose a new password.</p>
    <form method="post">
        <?= $form->fetch() ?>
        <button class="primary" type="submit">Send the link</button>
    </form>
    <p class="links"><a href="<?= esc_attr(route_url('/login')) ?>">Back to logging in</a></p>
<?php $this->endBlock() ?>
