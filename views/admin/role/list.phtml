<?php $this->useLayout('dpress:admin/layout') ?>
<?php $this->startBlock('content') ?>
    <div class="page-head">
        <h1>Roles</h1>
        <span class="spacer"></span>
        <?php if (!empty($can_create)): ?>
            <a class="button primary" href="<?= esc_attr($new_url) ?>">New</a>
        <?php endif ?>
    </div>

    <p class="form-description">
        The <strong>admin</strong> role holds every permission, including ones a plugin invents
        later, so it has nothing to tick and cannot be removed.
    </p>

    <?= $this->fetch('dpress:admin/list', ['list_id' => $list_id, 'list_config' => $list_config]) ?>
<?php $this->endBlock() ?>
