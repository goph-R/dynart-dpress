<?php $this->useLayout($admin_layout) ?>
<?php $this->startBlock('content') ?>
    <div class="page-head">
        <h1>Upload</h1>
        <span class="spacer"></span>
        <a class="button" href="<?= esc_attr($back_url) ?>">Back</a>
    </div>
    <div class="panel">
        <p class="form-description">
            An SVG is a document rather than a picture, so one is <strong>sanitised on the way
            in</strong>: scripts, event handlers and references to other sites are removed, and
            what gets stored is the cleaned file. A drawing survives that; a drawing that also
            wanted to run something does not.
        </p>
        <form method="post" enctype="multipart/form-data">
            <?= $form->fetch() ?>
            <p class="form-description">
                At most <?= esc_html($max_size) ?>. Allowed: <?= esc_html(implode(', ', $allowed)) ?>.
            </p>
            <div class="form-actions">
                <button type="submit" class="primary">Upload</button>
                <a class="button" href="<?= esc_attr($back_url) ?>">Cancel</a>
            </div>
        </form>
    </div>
<?php $this->endBlock() ?>
