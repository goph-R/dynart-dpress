<input type="search" name="search" placeholder="Search files...">
<select name="category">
    <option value="">Any kind</option>
    <?php foreach ($categories as $category): ?>
        <option value="<?= esc_attr($category) ?>"><?= esc_html(ucfirst($category)) ?></option>
    <?php endforeach ?>
</select>
<label class="inline"><input type="checkbox" name="with_deleted" value="1"> Show deleted</label>
