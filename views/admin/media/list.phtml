<?php $this->useLayout('dpress:admin/layout') ?>
<?php $this->startBlock('content') ?>
    <div class="page-head">
        <h1>Media</h1>
        <span class="spacer"></span>
        <?php if (!empty($can_upload)): ?>
            <a class="button primary" href="<?= esc_attr($upload_url) ?>">Upload</a>
        <?php endif ?>
    </div>

    <?= $this->fetch('dpress:admin/list', [
        'list_id' => $list_id,
        'list_config' => $list_config,
        'filters' => $this->fetch('dpress:admin/media/filters', ['categories' => $categories]),
    ]) ?>
<?php $this->endBlock() ?>
