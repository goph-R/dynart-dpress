<?php
/**
 * Publish: an eye, because publishing is what makes a document visible
 *
 * `icon-unpublish.svg` is the same eye crossed out, so the pair reads as one state and its
 * opposite - which is what they are, on the same row, in the same place.
 */
?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7-10-7-10-7z"/><circle cx="12" cy="12" r="3"/></svg>
