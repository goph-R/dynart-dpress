<?php
/**
 * The fallback navigation icon
 *
 * A section that names an icon this admin does not have - a plugin's own, most likely - gets this
 * rather than a gap, so the labels stay on one vertical line.
 */
?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="8"/></svg>
