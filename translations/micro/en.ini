; Overrides the framework's built in form messages with wording meant for a visitor
; rather than a developer. Registered under the `micro` namespace, which replaces the
; framework's own folder for it.

form_required     = "Required."
form_csrf_invalid = "Your session expired, please try again."
